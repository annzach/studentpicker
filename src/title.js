import React from 'react'
const Title = React.createClass ({//creates a new component
  render: function() {
    return(
      <div>
        <h1>STUDENT PICKER</h1>
      </div>
    );
  }
});
export default Title